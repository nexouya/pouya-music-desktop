# راهنمای ساخت خروجی دسکتاپ (Windows / Mac / Linux) با Electron

پروژه **Pouya Music** اکنون به‌طور کامل آماده تبدیل به برنامه نیتیو دسکتاپ است. تمامی پیش‌نیازها و اسکریپت‌های لازم در پروژه تنظیم شده‌اند.

---

## 🚀 نحوه تست و اجرای نسخه دسکتاپ روی سیستم خودتان:

### ۱. نصب وابستگی‌ها
در ترمینال سیستم خود در پوشه پروژه دستور زیر را اجرا کنید:
```bash
npm install
```

### ۲. تست نسخه دسکتاپ در حالت توسعه (Dev Mode)
برای اجرای پنجره دسکتاپ به همراه هات‌لود:
```bash
npm run electron:dev
```

---

## 📦 نحوه ساخت فایل نصبی خروجی (.exe یا .dmg):

### 🪟 برای ویندوز (تولید فایل Installer نصبی و Portable):
```bash
npm run dist:win
```
- **نتیجه:** فایل خروجی در پوشه `release/` با فرمت `.exe` (هم فایل Setup نصبی NSIS و هم نسخه بدون نیاز به نصب Portable) تولید می‌شود.

### 🍎 برای مک (macOS):
```bash
npm run dist:mac
```
- **نتیجه:** فایل `.dmg` در پوشه `release/` تولید می‌شود (با افکت شفاف شیشه‌ای اختصاصی macOS Vibrancy).

### 🐧 برای لینوکس (Linux):
```bash
npm run dist:linux
```
- **نتیجه:** پکیج‌های `AppImage` و `deb.` در پوشه `release/` قرار می‌گیرند.

---

## ✨ ویژگی‌های بهینه‌سازی شده برای نسخه دسکتاپ:
1. **استخراج و استفاده خودکار از کوکی لاگین کروم (Chrome / Edge / Brave):**
   - برنامه دسکتاپ به صورت هوشمند مرورگر پیش‌فرض سیستم شما (گوگل کروم یا مایکروسافت اج) را شناسایی می‌کند و در صورتی که در کروم وارد یوتیوب شده باشید، کوکی لاگین و نشست فعال شما (`--cookies-from-browser chrome`) را به طور مستقیم و خودکار برای استریم موزیک‌ها استفاده می‌کند!
   - این قابلیت کپچا و محدودیت‌های سن یا منطقه یوتیوب را به کلی برطرف می‌کند.
2. **بدون محدودیت آی‌پی:** درخواست‌های استریم و جستجو مستقیماً با آی‌پی واقعی کامپیوتر خودتان ارسال می‌شود.
3. **پشتیبانی از کلیدهای مالتی‌مدیا کیبورد:** کلیدهای فیزیکی Play/Pause/Next/Prev و کنترل‌سنتر ویندوز و مک (MediaSession API) به پلیر متصل هستند.
4. **سرور داخلی Embedded:** سرور Express و هسته صوتی مستقیماً در پس‌زمینه اپ دسکتاپ به صورت خودکار اجرا و مدیریت می‌شوند.
5. **کنترل‌های پنجره بومی:** دکمه‌های Minimize، Maximize و Close هماهنگ با طراحی مدرن اپل و ویندوز ۱۱ تعبیه شده‌اند.
